#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

typedef struct dif
{
	char *dif
	void (*a)(int a, int b);

}dif_p;

int _printf(const char *format, ...);

#endif
